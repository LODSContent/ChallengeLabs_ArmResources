This repo contains an ARM template that deploys a Windows Azure virtual machine and then uses Desired State Configuration (DSC)
to install IIS and configure a web page that displays the Azure VM name.
